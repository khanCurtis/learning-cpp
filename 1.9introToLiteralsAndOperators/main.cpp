#include <iostream>
using namespace std;

int main() {
    cout << 5 << '\n';  // prints the value of a literal

    int x { 5 };
    cout << x << '\n';  // prints the value of a variable

    return 0;
}